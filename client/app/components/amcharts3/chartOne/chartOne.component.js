import chartOneTemplate from './chartOne.template.html';
import chartOneController from './chartOne.controller';
import './chartOne.scss';

let chartOneComponent = {
  template: chartOneTemplate,
  controller: chartOneController
};

export default chartOneComponent;
